environment: stage

deployment:
  image:
    tag: 819
  replicas: 1

# COMO HAGO PARA QUE SE DIRECCIONE A DISTIONS AMBIENTES SI TENGO UN SOLO URL DEL ISTIO GATEWAY?????
host: a333fe17557854f4e9b85099a9154bb1-1599068481.us-east-2.elb.amazonaws.com # This value was modified by the deploy-infra pipeline

configmap: 
  nginxConf: |-
    server {
        listen       80;
        listen  [::]:80;
        server_name  localhost;
    
        location / {
            root   /usr/share/nginx/html;
            index  index.html index.htm;
        }
    
        location /api {
            # Now that we use Istio, we need to specify the http version to 1.1 for it to work (https://www.reddit.com/r/devops/comments/k0ooaw/envoy_426_http_issue/)
            proxy_http_version 1.1;
            
            proxy_pass http://mocha-backend-stage-deployment:3000/; # This value was modified by the initial-setup python script
        }
        
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   /usr/share/nginx/html;
        }
    }
